---
- hosts: all
  connection: local

  pre_tasks:
    - name: Include host-specific configuration variables
      include_vars: "{{ item }}"
      with_fileglob:
        - "config.{{ ansible_hostname }}.yml"
      tags: ['always']

  roles:
    - role: 'roles/packages'
    - role: 'roles/dock'
    - role: 'roles/terminal'
    - role: 'roles/osx'
    - role: 'roles/yadm'
    - role: 'roles/powershell'
